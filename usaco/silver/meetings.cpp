#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    freopen("meetings.in", "r", stdin);
    freopen("meetings.out", "w", stdout);
    cin.tie(0);
    ios::sync_with_stdio(0);
    int N, L; cin >> N >> L;
    for (int i = 0; i < N; ++i) {

    }

}
