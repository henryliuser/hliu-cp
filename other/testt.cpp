#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int N, C; cin >> N >> C;
    vector<int> A(N), B(N-1);
    for (int &x : A) cin >> x;
    for (int &x : B) cin >> x;

}
