// https://codeforces.com/problemset/problem/1556/C
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int n; cin >> n;
    vector<int> A(n);
    for (int &x : A)
        cin >> x;

    vector<char> st;
    for (int i = 0; i < n; ++i) {
        for (int j = i; j < n; ++j) {
            st.push_back()
        }
    }
}
