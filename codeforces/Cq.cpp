#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int solve() {
    
}

int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);
    int T; cin >> T;
    while (T--)
        cout << solve() << "\n";
}
